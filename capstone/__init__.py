"""The capstone ticket-triage project.

Deliberately self-contained — it doesn't import from `examples/`, because a
real project doesn't reach into a tutorial's scaffolding for its client
code. See `capstone/client.py` for the (small, duplicated-on-purpose)
Jev/LLM wrappers.
"""
